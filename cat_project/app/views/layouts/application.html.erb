<!DOCTYPE html>
<html>
<head>
  <title>CatProject</title>
  <%= csrf_meta_tags %>
</head>
<body>
  <% if flash.now[:errors] %>
    <%= flash.now[:errors] %>
  <% end %><br>
  <% if !current_user.nil? %>
  <ul>
    <li>Logged in as: <%= current_user.user_name %></li>
    <li><%= button_to "Logout", session_url, method: :delete %></li>
  </ul>
  <% else %>
    <li>Not Currently Logged In</li>
    <li><%= button_to "Log In", new_session_url, method: :get %></li>
    <li><%= button_to "Create New Account", new_user_url, method: :get %></li>
  <% end %>
  <!-- Params: <%= debug params %> -->
<%= yield %>

<a href= "<%=cats_url%>">Show all cats</a>
<a href= "<%=new_cat_url%>">Create a new cat!</a>
<a href= "<%=new_cat_rental_request_url%>">Reserve a cat!</a>


</body>
</html>

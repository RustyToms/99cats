<h1>All the Cats!</h1>

<ul>
  <% @cats.each do |cat| %>
    <li>
      <%= "#{cat.name} -  "%>
      <a href= "<%= cat_url(cat)%>">Learn more!</a>
      <%=cat.pic_html.html_safe%>
    </li>
  <% end %>
</ul>

<a href= "<%=new_user_url%>">Create New User</a>

